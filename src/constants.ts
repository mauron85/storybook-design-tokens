export const ADDON_ID = 'storybook/design-tokens';
export const PANEL_ID = `${ADDON_ID}/panel`;
export const PANEL_TITLE = 'Design tokens';

export const EVENTS = {
  UPDATE: `${ADDON_ID}/update`
} as const;
